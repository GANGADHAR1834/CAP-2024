class Solution:
    def firstUniqChar(self, s: str) -> int:
        for i in range(0,len(s)):
            a=s[i]
            if s.count(a)==1:
                return i
        return -1
            
            
